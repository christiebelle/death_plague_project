import React from 'react';

const Tombstone = (props) =>
<button onClick= {this.handleButtonClick} >{props.value}</button>

const  handleButtonClick = (event) => {
  <div height="500px">
      <h2>{this.props.title}</h2>
      <h4>{this.props.stat}</h4>
      <p>{this.props.info}</p>
  </div>
  };

export default Tombstone;
